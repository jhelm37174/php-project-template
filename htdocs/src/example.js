/**
 * Example function used to demonstrate Jasmine tests
 * @param  {int} varA a number
 * @param  {int} varB a number
 * @param  {int} varC a number
 * @return {int}      the total of a,b and c.
 */
var exampleFunction = function (varA, varB, varC) {
  var total = varA + varB + varC;
  return total;
};
